# GitHub Personal Access Token
# Get your token from: https://github.com/settings/tokens
# Required scopes: repo (or public_repo), read:org, read:user, user:email
GITHUB_PERSONAL_ACCESS_TOKEN=your_github_token_here

# GitHub Organization Name
# Example: microsoft, google, facebook
GITHUB_ORG_NAME=your_org_name

# Number of days to look back for contributor activity
# Example: 30 (for last 30 days)
NUMBER_OF_DAYS=30

# Note: Output files are automatically generated with format:
# outputs/<org_name>__<unix_timestamp>__contributor_count.json
# Example: outputs/microsoft__1730131800__contributor_count.json

# Optional: Comma-separated list of specific repositories to analyze
# If not specified or empty, all repositories in the organization will be analyzed
# Format: Just repository names (not owner/repo format)
# Case-insensitive matching (TypeScript matches typescript)
# Example: typescript,vscode,playwright
INTERESTING_REPOS=
